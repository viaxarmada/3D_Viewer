# 3D Volume Calculator - Dependencies

# Core framework
streamlit>=1.28.0

# 3D mesh processing
trimesh>=3.20.0
numpy>=1.24.0
networkx>=2.8

# Texture image decoding (also a transitive trimesh dep; pinned for clarity)
pillow>=9.0.0
